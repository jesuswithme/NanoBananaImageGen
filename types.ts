
export interface ImageData {
  base64: string;
  mimeType: string;
  width: number;
  height: number;
}

export interface GenerationOption {
    id: string;
    label: string;
    description: string;
}
